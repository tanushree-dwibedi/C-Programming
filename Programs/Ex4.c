
/*This is a comment? */
/*This one seems like a comment  doesn't it */
/* 
 * Calculate and display the difference of two input values 
 */
 #include <stdio.h> 
 int //this dosent show an error but it probably shouldnt be here
 main(void) {int X, /* first input value */ x, /* second 
 input value */ 
 sum; /* sum of inputs */ 
 scanf("%i%i", &X,&x); 
 sum = X + x ; 
 printf("%d + %d = %d\n", X, x, sum); 

    // Declare variables to store the values
    char letter;
    float number;

    // Store the values in variables
    letter = 'X';
    number = 76.1;

    // Display the stored values
    printf("Stored character: %c\n", letter);
    printf("Stored number: %.1f\n", number);


 return (0);

 } 